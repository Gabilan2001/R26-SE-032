# ML Models

This folder stores trained model artifacts used by the price prediction service.

- `lstm_price_predictor.h5` - TensorFlow/Keras LSTM model file for tomato price forecasting.

Place the trained model here before starting the service.
