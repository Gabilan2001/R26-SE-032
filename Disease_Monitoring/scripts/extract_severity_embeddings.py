"""Offline script placeholder: extract severity embeddings for research clustering.

Does not run automatically. Requires processed dataset paths outside this repo.
"""

if __name__ == "__main__":
    print(
        "Offline research step only. Configure dataset paths and run manually "
        "after YOLO/CNN processed data is available."
    )
