"""Offline script placeholder: PCA/K-Means clustering for automatic relative severity groups."""

if __name__ == "__main__":
    print(
        "Offline research step only. Document clustering methodology before running. "
        "Do not auto-retrain EfficientNet from this script."
    )
