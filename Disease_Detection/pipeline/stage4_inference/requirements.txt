# Requirements for app_mobile.py (production backend: detection + RAG treatment)
# Pinned to versions verified working in local end-to-end testing.
#
# CPU-only torch/torchvision -- Cloud Run has no GPU, and the CUDA wheels
# (used locally, torch==2.5.1+cu121) are ~2GB heavier for nothing.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1

flask==3.1.3
flask-cors==6.0.2
gunicorn==23.0.0

# headless -- no GUI (Qt/GTK) bindings needed on a server, avoids missing
# libGL errors in the slim base image. Same API as opencv-python.
opencv-python-headless==4.13.0.92
numpy==2.4.4
pillow==12.2.0

ultralytics==8.4.45
rembg==2.0.75

python-dotenv==1.2.2
chromadb==1.5.9
sentence-transformers==5.5.0
google-genai==2.2.0

# Scan history/dashboard (independent DB, no Nutrient/Fruit auth dependency)
google-cloud-firestore==2.20.2
